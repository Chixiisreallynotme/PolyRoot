// via setup-ts-deep-modules: entrypoint public seul — NEVER import lib/ dehors
export { ObjectPool } from '../../core/ObjectPool'
export type { Brand, ChipId, EntityId, PlayerId, Result } from './lib/types'
export { ok, err } from './lib/types'
